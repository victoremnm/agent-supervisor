# Gemini (primary model — free tier at aistudio.google.com)
GEMINI_API_KEY=AIza...          # From aistudio.google.com -> Get API key

# GitHub App (see GITHUB_APP_SETUP.md for creation steps)
GITHUB_APP_ID=123456            # From GitHub App settings page
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n..."  # PEM key (single line, \n escaped)

# Multi-org: one installation per GitHub account/org (comma-separated owner:id pairs)
GITHUB_APP_INSTALLATION_IDS=yourname:111111,YourOrg:222222,OtherOrg:333333

# Single-org fallback (used if owner not found in IDS map above)
GITHUB_APP_INSTALLATION_ID=    # From: gh api /repos/<owner>/<repo>/installation --jq '.id'

# Target repositories (comma-separated owner/repo pairs)
TARGET_REPOS=yourname/repo1,YourOrg/repo2,OtherOrg/repo3

# Langfuse observability (self-hosted, see docker-compose.yml)
LANGFUSE_SECRET_KEY=sk-lf-...  # From Langfuse dashboard -> Settings -> API Keys
LANGFUSE_PUBLIC_KEY=pk-lf-...  # From Langfuse dashboard -> Settings -> API Keys
LANGFUSE_HOST=http://localhost:3000
